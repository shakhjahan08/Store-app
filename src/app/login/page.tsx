"use client";
import { FormEvent, useState } from "react";
import { useRouter } from "next/navigation";

export default function LoginPage() { const router = useRouter(); const [role, setRole] = useState<"client" | "seller">("client"); const [name, setName] = useState("John Smith"); const submit = (event: FormEvent) => { event.preventDefault(); localStorage.setItem("storeapp-user", JSON.stringify({ name, role })); router.push(role === "seller" ? "/dashboard" : "/store"); }; return <main className="flex min-h-screen items-center justify-center bg-slate-950 px-4"><form onSubmit={submit} className="w-full max-w-md rounded-3xl border border-slate-800 bg-slate-900 p-8"><h1 className="text-3xl font-bold">Welcome to StoreApp</h1><p className="mt-2 text-slate-400">Choose your marketplace experience.</p><label className="mt-8 block text-sm text-slate-300">Full name<input required value={name} onChange={(e) => setName(e.target.value)} className="mt-2 w-full rounded-xl border border-slate-700 bg-slate-800 px-4 py-3 outline-none" /></label><div className="mt-5 grid grid-cols-2 gap-3"><button type="button" onClick={() => setRole("client")} className={`rounded-xl px-4 py-3 font-medium ${role === "client" ? "bg-blue-600" : "bg-slate-800 text-slate-300"}`}>Client</button><button type="button" onClick={() => setRole("seller")} className={`rounded-xl px-4 py-3 font-medium ${role === "seller" ? "bg-blue-600" : "bg-slate-800 text-slate-300"}`}>Seller</button></div><button className="mt-5 w-full rounded-xl bg-blue-600 px-4 py-3 font-semibold">Continue as {role}</button></form></main>; }
