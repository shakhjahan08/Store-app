"use client";
import Link from "next/link";
import { useEffect, useMemo, useState } from "react";
import { products } from "@/lib/demo-data";

export default function CartPage() { const [cart, setCart] = useState<{ id: number; qty: number }[]>([]); useEffect(() => { const saved = localStorage.getItem("storeapp-cart"); if (saved) setCart(JSON.parse(saved)); }, []); useEffect(() => { localStorage.setItem("storeapp-cart", JSON.stringify(cart)); }, [cart]); const items = useMemo(() => cart.map((item) => { const product = products.find((p) => p.id === item.id); return product ? { ...product, qty: item.qty } : null; }).filter(Boolean) as Array<(typeof products)[number] & { qty: number }>, [cart]); const subtotal = items.reduce((sum, item) => sum + item.price * item.qty, 0); const update = (id: number, delta: number) => setCart((current) => current.map((item) => item.id === id ? { ...item, qty: Math.max(0, item.qty + delta) } : item).filter((item) => item.qty > 0)); return <main className="min-h-screen bg-slate-950 px-6 py-10"><div className="mx-auto max-w-6xl"><div className="mb-8 flex items-center justify-between"><h1 className="text-4xl font-bold">Your cart</h1><Link href="/store" className="rounded-full border border-slate-700 px-4 py-2">Continue shopping</Link></div><div className="grid gap-8 lg:grid-cols-3"><div className="space-y-5 lg:col-span-2">{items.length === 0 ? <div className="rounded-3xl border border-dashed border-slate-700 bg-slate-900 p-10 text-center text-slate-400">Your cart is empty.</div> : items.map((item) => <div key={item.id} className="flex gap-4 rounded-3xl border border-slate-800 bg-slate-900 p-4"><img src={item.image} alt={item.name} className="h-28 w-28 rounded-2xl object-cover" /><div className="flex flex-1 items-center justify-between"><div><h2 className="text-xl font-semibold">{item.name}</h2><p className="text-slate-400">{item.seller}</p><p className="mt-2 font-bold">${item.price}</p></div><div className="flex items-center gap-3"><button onClick={() => update(item.id, -1)} className="h-8 w-8 rounded-full bg-slate-800">-</button><span>{item.qty}</span><button onClick={() => update(item.id, 1)} className="h-8 w-8 rounded-full bg-slate-800">+</button></div></div></div>)}</div><aside className="h-fit rounded-3xl border border-slate-800 bg-slate-900 p-6"><h2 className="text-2xl font-bold">Order summary</h2><div className="mt-6 flex justify-between text-slate-300"><span>Subtotal</span><span>${subtotal}</span></div><div className="mt-3 flex justify-between text-slate-300"><span>Shipping</span><span>${subtotal ? 12 : 0}</span></div><div className="mt-4 flex justify-between border-t border-slate-700 pt-4 text-xl font-semibold"><span>Total</span><span>${subtotal + (subtotal ? 12 : 0)}</span></div><button disabled={!items.length} className="mt-8 w-full rounded-xl bg-blue-600 px-4 py-3 font-semibold disabled:cursor-not-allowed disabled:opacity-50">Checkout</button></aside></div></div></main>; }
