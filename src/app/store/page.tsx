"use client";
import Link from "next/link";
import { useEffect, useMemo, useState } from "react";
import { products, Product } from "@/lib/demo-data";

export default function StorePage() { const [search, setSearch] = useState(""); const [category, setCategory] = useState("All"); const [cart, setCart] = useState<{ id: number; qty: number }[]>([]); const [name, setName] = useState("Guest"); useEffect(() => { const user = localStorage.getItem("storeapp-user"); if (user) setName(JSON.parse(user).name || "Guest"); const savedCart = localStorage.getItem("storeapp-cart"); if (savedCart) setCart(JSON.parse(savedCart)); }, []); useEffect(() => { localStorage.setItem("storeapp-cart", JSON.stringify(cart)); }, [cart]); const categories = ["All", ...new Set(products.map((p) => p.category))]; const filtered = useMemo(() => products.filter((p) => (category === "All" || p.category === category) && [p.name, p.description, p.seller].some((value) => value.toLowerCase().includes(search.toLowerCase()))), [search, category]); const add = (product: Product) => setCart((items) => { const found = items.find((item) => item.id === product.id); return found ? items.map((item) => item.id === product.id ? { ...item, qty: item.qty + 1 } : item) : [...items, { id: product.id, qty: 1 }]; }); const itemCount = cart.reduce((sum, item) => sum + item.qty, 0); return <main className="min-h-screen bg-slate-950"><nav className="mx-auto flex max-w-7xl items-center justify-between px-6 py-5"><Link href="/" className="text-2xl font-bold">StoreApp</Link><div className="flex items-center gap-4"><span className="text-sm text-slate-300">Hi, {name}</span><Link href="/cart" className="rounded-full bg-blue-600 px-4 py-2 font-semibold">Cart ({itemCount})</Link></div></nav><section className="mx-auto max-w-7xl px-6 py-10"><h1 className="text-4xl font-bold">Shop trending products</h1><p className="mt-2 text-slate-400">Discover items from trusted sellers.</p><div className="my-8 flex flex-col gap-4 md:flex-row"><input value={search} onChange={(e) => setSearch(e.target.value)} placeholder="Search products or sellers" className="flex-1 rounded-xl border border-slate-700 bg-slate-900 px-4 py-3 outline-none" /><div className="flex flex-wrap gap-2">{categories.map((item) => <button key={item} onClick={() => setCategory(item)} className={`rounded-full px-4 py-2 ${category === item ? "bg-blue-600" : "bg-slate-800 text-slate-300"}`}>{item}</button>)}</div></div><div className="grid gap-6 md:grid-cols-2 xl:grid-cols-3">{filtered.map((product) => <article key={product.id} className="overflow-hidden rounded-3xl border border-slate-800 bg-slate-900"><img src={product.image} alt={product.name} className="h-60 w-full object-cover" /><div className="p-5"><div className="flex justify-between text-sm text-slate-400"><span>{product.category}</span><span>{product.seller}</span></div><h2 className="mt-2 text-xl font-semibold">{product.name}</h2><p className="mt-2 text-sm text-slate-400">{product.description}</p><div className="mt-4 flex items-center justify-between"><div><p className="text-2xl font-bold">${product.price}</p><p className="text-xs text-slate-400">{product.stock} in stock</p></div><button onClick={() => add(product)} className="rounded-full bg-blue-600 px-4 py-2 font-semibold">Add to cart</button></div></div></article>)}</div></section></main>; }
